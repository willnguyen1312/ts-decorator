import { ReflectDemo, ReflectAnotherDemo } from '../src/reflection';

describe('ReflectDemo', () => {
  it('Obtaining type metadata', () => {
    new ReflectDemo();
  });

  it(' Obtaining Parameter type metadata', () => {
    new ReflectAnotherDemo();
  });
});
